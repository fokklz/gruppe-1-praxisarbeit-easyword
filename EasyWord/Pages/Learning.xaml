<Page x:Class="EasyWord.Pages.Learning"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:local="clr-namespace:EasyWord.Pages"
      mc:Ignorable="d" 
      Background="White" d:DesignWidth="804.515"
      Title="Learning" Height="436">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80*"/>
            <ColumnDefinition Width="245*"/>
            <ColumnDefinition Width="80*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="91*"/>
            <RowDefinition Height="100*"/>
            <RowDefinition Height="100*"/>
        </Grid.RowDefinitions>
        <!--Title-->
        <TextBlock
            Style="{StaticResource MaterialDesignHeadline6TextBlock}"
            Text="Bitte CSV importieren" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <!--Input button-->
        <Button
            Content="_INPUT CSV"
            IsEnabled="{Binding DataContext.ControlsEnabled, RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}"
            Style="{StaticResource MaterialDesignRaisedDarkButton}"
            ToolTip="Resource name: MaterialDesignRaisedLightDarkButton" Grid.Column="2" Margin="10,30,10,30" Grid.Row="0" RenderTransformOrigin="0.508,0.426" />
        <!--Textinput card-->
        <materialDesign:Card
            Padding="8"
            Background="{DynamicResource PrimaryHueLightBrush}"
            Foreground="{DynamicResource PrimaryHueLightForegroundBrush}" Grid.Column="1" Margin="10,4,10,10" Grid.Row="1">
            <TextBlock
                FontSize="20" TextAlignment="Center"
                ><Run Language="de-ch" Text="Test"/><LineBreak/><Run Language="de-ch"/></TextBlock>
        </materialDesign:Card>
        <!--Textoutput-->
        <StackPanel Grid.Column="1" Margin="10,5,10,60" Grid.Row="2" VerticalAlignment="Center">
            <TextBox
                materialDesign:HintAssist.Hint="English"
                materialDesign:TextFieldAssist.CharacterCounterVisibility="{Binding IsChecked, Converter={StaticResource BooleanToVisibilityConverter}, ElementName=MaterialDesignFilledTextBoxTextCountComboBox}"
                MaxLength="40"
                Style="{StaticResource MaterialDesignFilledTextBox}"
                TextWrapping="Wrap" />
        </StackPanel>
        <!--Button Check-->
        <Button Name="btnCheck" Click="btnCheck_Click" Grid.Column="2" Content="Check" HorizontalAlignment="Center" Grid.Row="2" VerticalAlignment="Top" Height="42" Width="102" Visibility="Visible" Margin="0,27,0,0" />
    </Grid>
</Page>
